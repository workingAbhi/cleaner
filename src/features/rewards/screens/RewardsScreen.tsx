import React from 'react';
import {
  Text,
  View,
} from 'react-native';

const RewardsScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>
        Rewards Screen
      </Text>
    </View>
  );
};

export default RewardsScreen;